{% extends 'base.html.twig' %}

{% block title %}Liste des tweets{% endblock %}

{% block body %}
    <h2>Liste des tweets</h2>
    {% for tweet in tweets %}
        <p>
            {{ tweet.text }}
            (posté le {{ tweet.postedAt | date('d/m/Y') }} à
            {{ tweet.postedAt | date('h:i:s', 'Europe/Paris') }})
        </p>
    {% endfor %}
{% endblock %}
